# 跨 profile 公共环境变量模板 —— 生成:~/.config/cca/common.env
# 生成方式:cca init common(即复制本模板),然后编辑填充。已有文件不会被覆盖。
# 生效范围:对所有 profile(含官方订阅)生效;profile 里同名键可覆盖;改完重启 cca。
# ⚠️ 含真实密钥,只存在本机 ~/.config/cca/,勿入库、勿明文外传。
# 以下变量默认都注释掉 = 惰性:没填不影响启动;填了哪条启用哪条。

# --- git 身份(所有 profile 生效)---
# 容器内不复用宿主机 ~/.gitconfig,不配则 git commit 报 "empty ident"。
# 用 `cca init official` 建官方 profile 前,也建议先把这四条配上。
# GIT_AUTHOR_NAME=你的名字
# GIT_AUTHOR_EMAIL=you@example.com
# GIT_COMMITTER_NAME=你的名字
# GIT_COMMITTER_EMAIL=you@example.com

# --- cc-vision skill:让 Claude 借 MiniMax-M3 看图 ---
# 仅当当前模型收不了图(如 DeepSeek 纯文本中转)、要用 cc-vision 时填。
# Key 在 MiniMax 开放平台申请(platform.minimaxi.com)。
# MINIMAX_API_KEY=在此填你的MiniMaxAPIKEY

# --- 可选:运行时代理(所有 profile 生效;境内中转一般不需要)---
# 容器内不能用 127.0.0.1,宿主机代理写 host.docker.internal。
# HTTP_PROXY=http://host.docker.internal:7897
# HTTPS_PROXY=http://host.docker.internal:7897
