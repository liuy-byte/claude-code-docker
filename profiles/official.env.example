# 官方订阅 profile —— 推荐用 `cca init official` 生成(自动 chmod 600)。
# 各键含义见 README《第三方中转》。
#
# 官方订阅不走 KEY,鉴权走 OAuth 首次登录(终端打印 URL → 浏览器授权)。
# 此文件存在的唯一常见理由 = 给境外 api.anthropic.com 配代理;
# 留空 = 等价于"无文件",首次启动照旧走 OAuth。

# 可选代理(境外官方订阅必填,否则连不通;宿主机代理写 host.docker.internal,不能写 127.0.0.1)
# HTTP_PROXY=http://host.docker.internal:7897
# HTTPS_PROXY=http://host.docker.internal:7897
